@model Empleado

@{
    ViewData["Title"] = "Detalles Empleado";
}

<h1>Detalles del Empleado</h1>

<dl class="row">
    <dt class="col-sm-2">Nombres</dt>
    <dd class="col-sm-10">@Model.Nombres</dd>
    <dt class="col-sm-2">Apellidos</dt>
    <dd class="col-sm-10">@Model.Apellidos</dd>
    <dt class="col-sm-2">Cargo</dt>
    <dd class="col-sm-10">@Model.Cargo</dd>
    <dt class="col-sm-2">Teléfono</dt>
    <dd class="col-sm-10">@Model.Telefono</dd>
    <dt class="col-sm-2">Email</dt>
    <dd class="col-sm-10">@Model.Email</dd>
    <dt class="col-sm-2">Salario</dt>
    <dd class="col-sm-10">@Model.Salario.ToString("C")</dd>
    <dt class="col-sm-2">Fecha de contratación</dt>
    <dd class="col-sm-10">@Model.FechaContratacion.ToString("d")</dd>
</dl>

<a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-secondary">Editar</a>
<a asp-action="Index" class="btn btn-primary">Volver a la lista</a>
